# SciAgent Public Wizard — Railway deployment configuration

[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
healthcheckPath = "/public/"
healthcheckTimeout = 120
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
startCommand = "hypercorn 'sciagent_wizard:create_production_app()' --bind 0.0.0.0:${PORT:-8080} --workers 1 --access-log - --error-log -"
